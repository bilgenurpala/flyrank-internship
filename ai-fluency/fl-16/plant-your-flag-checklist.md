# Plant Your Flag: Domain + Badge

## Portal acceptance check

| Requirement | Current evidence | Status |
|---|---|---|
| Final address over HTTPS | `https://bilgenurpala.netlify.app/` returns HTTP 200 over HTTPS and is suitable for the CV while the FlyRank subdomain remains unavailable. | Ready |
| Free analytics installed and receiving data | Google Analytics property `G-976MQ2G2BX` is wired through the shared layout. | Installed; production traffic verification required |
| Page titles and descriptions | Home, Work, About, and Contact emit route-specific titles and descriptions. | Ready in source; production retest required |
| Share preview | Open Graph and X card metadata are implemented with an absolute public image URL. | Ready in source; production unfurl evidence required |
| Favicon | SVG, 32 px, and Apple touch icons are configured. | Ready |
| Final address checked on a real phone | The four-route Netlify site was tested on a physical iPhone before the latest Work and Contact changes. | Retest required after deployment |
| FlyRank graduate badge | FlyRank has not issued the badge or verification URL. | Legitimately blocked until capstone approval |
| Portal submission | Submission is intentionally deferred until all program tasks are complete. | Not submitted |

## What is genuinely left

The 20-hour workload does not represent twenty new implementation hours for this portfolio. HTTPS hosting, the clean fallback URL, responsive pages, titles, descriptions, favicons, and Google Analytics integration already exist. The remaining work is operational evidence: deploy the analytics tag, confirm it receives a real visit, capture the public share preview, retest the final deployment on a phone, and add the issued badge only when FlyRank supplies both the asset and verification URL.

No badge placeholder should be published. The fallback Netlify address remains the valid submission address until the FlyRank subdomain is provisioned.
