# Still Ugly

This version is complete enough to navigate and inspect, but it is not finished.

- The Work page is long. Real visitor feedback must decide whether the case order is clear or whether each case needs its own route.
- A real visitor understood the FastAPI, PostgreSQL, RAG, and backend evidence but said the agent-focused headline is ahead of the work currently shown. The claim must be reassessed after SafeBump has real run evidence.
- The visitor could not tell whether the `System trace`, `Approved`, and `Ground / Build / Verify` graphics represented real telemetry or a design principle. Their meaning needs clearer visual context.
- The `8/10`, `7/10`, `51 assertions`, and `4 backend builds` summaries need more immediate context for readers who do not already know the evaluation sets or internship assignments.
- SafeBump is only an honest reserved case. Implementation has not started, so it has no build log, run evidence, repository, report, or demo.
- PetAdopt has repository and test evidence but no public live demo.
- The Local RAG case has a real interface capture and reproducible evaluation records, but the full model-backed application runs locally rather than on the public portfolio.
- The site has no demo videos yet.
- The LinkedIn, GitHub, CV, and booking links are live, but the CV content will be updated again after SafeBump is complete.
- The site uses Google Fonts, so its custom typography depends on a third-party font request. System-font fallbacks keep the content readable if that request fails.
- The final FlyRank subdomain is intentionally not requested before capstone approval. The current public address remains `https://bilgenurpala.netlify.app`.
