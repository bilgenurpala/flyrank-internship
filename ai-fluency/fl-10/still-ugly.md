# Still Ugly

This version is complete enough to navigate and inspect, but it is not finished.

- The Work page is long. Real visitor feedback must decide whether the case order is clear or whether each case needs its own route.
- SafeBump is only an honest reserved case. It has no build log, run evidence, repository, report, or demo until the implementation starts on 15 August 2026.
- PetAdopt has repository and test evidence but no public live demo.
- The Local RAG case has a real interface capture and reproducible evaluation records, but the full model-backed application runs locally rather than on the public portfolio.
- The site has no demo videos yet.
- The LinkedIn, GitHub, CV, and booking links exist in the local site update but must be verified again after deployment.
- The site uses Google Fonts, so its custom typography depends on a third-party font request. System-font fallbacks keep the content readable if that request fails.
- The final FlyRank subdomain is intentionally not requested before capstone approval. The current public address remains `https://bilgenurpala.netlify.app`.
